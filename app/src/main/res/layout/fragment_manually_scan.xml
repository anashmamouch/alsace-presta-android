<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/btn_entry"
    android:orientation="vertical" >

    <LinearLayout
        android:id="@+id/Scan_manually_layout_1"
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="4"        android:gravity="center"
        android:orientation="vertical" >

        <AutoCompleteTextView
            android:id="@+id/numotipass"
            style="@style/edit_text_init"
            android:ems="10"
            android:hint="@string/otipass_num"
            android:imeOptions="actionSearch"
            android:inputType="number"
            android:maxLength="8" >
        </AutoCompleteTextView>
    </LinearLayout>

    <RelativeLayout
        android:id="@+id/Scan_manually_layout_2"
        android:layout_width="fill_parent"
        android:layout_height="50dp"
        android:layout_weight="5"
        android:background="@color/menu_bar_bg"
        android:orientation="horizontal" >

        <Button
            android:id="@+id/btn_return"
            style="@style/button_back.enabled" />

        <Button
            android:id="@+id/btn_Cancel"
            style="@style/button_cancel.enabled" />

        <Button
            android:id="@+id/btn_Valid"
            style="@style/button_valid.enabled" />
    </RelativeLayout>

</LinearLayout>